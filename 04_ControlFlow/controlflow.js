function checkNumber(num) {
    if (num > 0) {
        console.log("The number is positive.");
    } else if (num < 0) {
        console.log("The number is negative.");
    } else {
        console.log("The number is zero.");
    }
}

// Example usage:
checkNumber(5);   // The number is positive.
checkNumber(-3);  // The number is negative.
checkNumber(0);   // The number is zero.

if (true) console.log("This will always execute."),console.log("So will this.");  // both can eccute but it not a good practice to write code like this 
// Switch statement example
